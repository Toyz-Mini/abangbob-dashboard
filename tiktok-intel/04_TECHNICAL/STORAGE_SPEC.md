# Storage Specification

Raw files are stored separately from normalized records.

Storage path is tenant-scoped.
Downloads require authorization.
Generated filenames prevent path manipulation.
Retention and deletion apply to original, processed and generated files.
