# Data Mapping Specification

## Mapping states
AUTO_MAPPED → USER_CONFIRMED → VALIDATED.

Ambiguous mappings require confirmation.

Mapping templates are scoped to workspace/shop/source schema version.

Manual overrides are audited and never globally alter canonical definitions.
