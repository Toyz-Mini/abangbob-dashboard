# Metric Change Control

Any platform metric-definition change follows:

Source change detected
→ platform knowledge update
→ metric version update
→ affected rule discovery
→ affected UI/API discovery
→ golden test update
→ backfill decision
→ review
→ release.

Historical analysis retains the metric version used at the time.

No metric definition may be changed silently.
