# Audit Logging

Audit:
login/security events, shop creation/deletion, imports, mapping overrides, exports, permission changes, diagnosis creation and data deletion.

Record actor, action, entity, timestamp, result and correlation ID.
