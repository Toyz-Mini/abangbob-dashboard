# Regression Testing

A rule, metric or prompt release must run all affected golden datasets.

Unexpected changes to diagnosis, priority or confidence block release unless explicitly approved and versioned.
